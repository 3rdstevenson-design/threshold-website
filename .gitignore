# Dependencies
node_modules/
/.pnp
.pnp.js

# Build output
/.next/
/out/
/build

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Claude Code local settings
.claude/

# Local queue fallback (Vercel Blob is the real store)
data/queue.json
data/analytics.json

# Runtime state: offline queue snapshot, in-flight editor jobs, watcher state
data/queue-cache.json
data/editor-jobs.json
data/watch-renders-state.json

# ManyChat Playwright session (contains auth cookies)
tools/.manychat-session/

# ManyChat config files (may contain DM text and private links)
tools/manychat-configs/

# Python venv for reframe.py (MediaPipe)
.venv/

# isolated production build dir (always-on launchd service)
.next-prod/

# Content-plan source screenshots (large PNGs regenerated by the brief job)
content-plans/daily/sources/

# Ad-hoc queue dumps
data/queue-backup-*.json
.next-check/
